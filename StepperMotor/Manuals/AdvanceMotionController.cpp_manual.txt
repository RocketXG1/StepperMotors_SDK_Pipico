MANUAL ACTUALIZADO - src/srcMotion/AdvanceMotionController.cpp
================================================================================
Funcion inicial del archivo
- Coordina ejes, HOME, validaciones, cola, perfil y ejecucion sincronizada.

Metodos, variables, secciones o instancias detectadas
- L11: `axisCount = 0;`
- L13: `pulseEngine = &selectedPulseEngine;`
- L15: `validController = false;`
- L16: `busy = false;`
- L18: `dominantStepCounter = 0;`
- L20: `nextStepTime = get_absolute_time();`
- L22: `homeOrderCount = 0;`
- L23: `homeOrderConfigured = false;`
- L25: `for (uint i = 0; i < MAX_AXES; i++) {`
- L26: `axes[i] = nullptr;`
- L27: `bresenhamError[i] = 0;`
- L28: `homeOrder[i] = 0;`
- L31: `currentBlock.reset();`
- L34: `validController = true;`
- L38: `validController =`
- L39: `validateAxesStepPins();`
- L43: `validController =`
- L44: `validateAxisNames();`
- L49: `plannerConfig.blendEnabled = false;`
- L50: `plannerConfig.blendDistanceMm = 1.0f;`
- L51: `plannerConfig.blendSegmentLengthMm = 0.5f;`
- L52: `plannerConfig.maxBlendSegments = 8;`
- L53: `plannerConfig.maxTrimRatio = 0.35f;`
- L54: `plannerConfig.minMovementMm = 0.01f;`
- L55: `plannerConfig.blendSpeedScale = 0.70f;`
- L69: `if (axisList == nullptr) {`
- L81: `for (uint i = 0; i < numberOfAxes; i++) {`
- L82: `if (axisList[i] == nullptr) {`
- L86: `axes[i] = axisList[i];`
- L89: `axisCount = numberOfAxes;`
- L94: `bool AdvanceMotionController::validateAxesStepPins() {`
- L95: `if (pulseEngine == nullptr) {`
- L101: `for (uint i = 0; i < MAX_AXES; i++) {`
- L102: `usedOffsets[i] = false;`
- L105: `for (uint i = 0; i < axisCount; i++) {`
- L106: `uint stepPin =`
- L107: `axes[i]->getStepPin();`
- L113: `uint offset =`
- L114: `pulseEngine->getPinOffset(stepPin);`
- L116: `if (offset >= MAX_AXES) {`
- L124: `usedOffsets[offset] = true;`
- L134: `if (firstText == nullptr || secondText == nullptr) {`
- L138: `uint index = 0;`
- L141: `firstText[index] != '\0' ||`
- L142: `secondText[index] != '\0'`
- L144: `if (firstText[index] != secondText[index]) {`
- L157: `if (axisName == nullptr) {`
- L161: `if (axisName[0] == '\0') {`
- L172: `bool AdvanceMotionController::validateAxisNames() const {`
- L173: `for (uint i = 0; i < axisCount; i++) {`
- L174: `const char* currentName =`
- L175: `axes[i]->getAxisName();`
- L181: `for (uint j = i + 1; j < axisCount; j++) {`
- L182: `const char* compareName =`
- L183: `axes[j]->getAxisName();`
- L199: `if (inputName == nullptr) {`
- L203: `if (outputName == nullptr) {`
- L207: `if (outputSize == 0) {`
- L211: `uint writeIndex = 0;`
- L212: `bool hasValidCharacter = false;`
- L215: `uint readIndex = 0;`
- L216: `inputName[readIndex] != '\0' &&`
- L217: `writeIndex < (outputSize - 1);`
- L220: `char currentChar =`
- L224: `currentChar == ' ' ||`
- L225: `currentChar == '\t' ||`
- L226: `currentChar == '\r' ||`
- L227: `currentChar == '\n'`
- L232: `if (currentChar >= 'a' && currentChar <= 'z') {`
- L233: `currentChar =`
- L234: `currentChar - ('a' - 'A');`
- L237: `outputName[writeIndex] =`
- L240: `hasValidCharacter = true;`
- L244: `outputName[writeIndex] = '\0';`
- L263: `for (uint i = 0; i < axisCount; i++) {`
- L265: `axes[i]->getAxisName(),`
- L268: `outputAxisIndex = i;`
- L276: `void AdvanceMotionController::init() {`
- L281: `if (pulseEngine != nullptr) {`
- L282: `pulseEngine->init();`
- L285: `for (uint i = 0; i < axisCount; i++) {`
- L286: `axes[i]->init();`
- L290: `bool AdvanceMotionController::isValid() const {`
- L294: `uint AdvanceMotionController::getAxisCount() const {`
- L301: `if (axisIndex >= axisCount) {`
- L305: `if (axes[axisIndex] == nullptr) {`
- L309: `return axes[axisIndex]->getAxisName();`
- L312: `void AdvanceMotionController::printAxisMap() const {`
- L313: `printf("\n===== AXIS MAP =====\n");`
- L316: `printf("Controller invalid.\n");`
- L317: `printf("====================\n\n");`
- L321: `for (uint i = 0; i < axisCount; i++) {`
- L323: `"Index [%u] = Axis %s | STEP GPIO %u | DIR GPIO %u | ENABLE GPIO %u\n",`
- L325: `axes[i]->getAxisName(),`
- L326: `axes[i]->getStepPin(),`
- L327: `axes[i]->getDirPin(),`
- L328: `axes[i]->getEnablePin()`
- L332: `printf("====================\n\n");`
- L335: `void AdvanceMotionController::printHomeOrder() const {`
- L336: `printf("\n===== HOME ORDER =====\n");`
- L339: `printf("Home order not configured.\n");`
- L340: `printf("======================\n\n");`
- L344: `for (uint i = 0; i < homeOrderCount; i++) {`
- L345: `uint axisIndex =`
- L349: `"Home step %u: Axis index [%u] = %s\n",`
- L352: `getAxisName(axisIndex)`
- L356: `printf("======================\n\n");`
- L363: `float sum = 0.0f;`
- L365: `for (uint i = 0; i < count; i++) {`
- L366: `sum += deltas[i] * deltas[i];`
- L369: `return sqrtf(sum);`
- L380: `block.reset();`
- L386: `if (targetPositionsMm == nullptr) {`
- L390: `if (targetCount != axisCount) {`
- L394: `if (requestedSpeedMmS <= 0.0f) {`
- L398: `block.axisCount = axisCount;`
- L399: `block.requestedSpeedMmS = requestedSpeedMmS;`
- L400: `block.entrySpeedMmS = entrySpeedMmS;`
- L401: `block.exitSpeedMmS = exitSpeedMmS;`
- L403: `uint32_t dominantSteps = 0;`

Quien lo manda a llamar / relacion con otros archivos
- Los headers en lib son incluidos por implementaciones en src y por prgProject/main.cpp.
- Los .cpp son compilados por prgProject/CMakeLists.txt y sus funciones se ejecutan desde main.cpp o desde otros objetos del controlador.
- Los archivos config/recipes/*.txt son consumidos por RecipeLoader/SdRecipeStorage/MachineRecipeParser cuando se carga una receta.
- Los manuales son referencia de uso y no participan en la compilacion.

Explicacion linea por linea
| Linea | Codigo | Explicacion |
|---:|---|---|
| 1 | `#include "AdvanceMotionController.h"` | Incluye una dependencia necesaria para declaraciones, tipos o funciones usadas en este archivo. |
| 2 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 3 | `#include <math.h>` | Incluye una dependencia necesaria para declaraciones, tipos o funciones usadas en este archivo. |
| 4 | `#include <stdio.h>` | Incluye una dependencia necesaria para declaraciones, tipos o funciones usadas en este archivo. |
| 5 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 6 | `AdvanceMotionController::AdvanceMotionController(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 7 | `    AdvanceStepperAxis* axisList[],` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 8 | `    uint numberOfAxes,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 9 | `    StepperPioPulseEngine& selectedPulseEngine` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 10 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 11 | `    axisCount = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 12 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 13 | `    pulseEngine = &selectedPulseEngine;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 14 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 15 | `    validController = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 16 | `    busy = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 17 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 18 | `    dominantStepCounter = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 19 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 20 | `    nextStepTime = get_absolute_time();` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 21 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 22 | `    homeOrderCount = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 23 | `    homeOrderConfigured = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 24 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 25 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 26 | `        axes[i] = nullptr;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 27 | `        bresenhamError[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 28 | `        homeOrder[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 29 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 30 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 31 | `    currentBlock.reset();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 32 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 33 | `    if (loadAxes(axisList, numberOfAxes)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 34 | `        validController = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 35 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 36 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 37 | `    if (validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 38 | `        validController =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 39 | `            validateAxesStepPins();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 40 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 41 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 42 | `    if (validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 43 | `        validController =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 44 | `            validateAxisNames();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 45 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 46 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 47 | `    MotionPlannerConfig plannerConfig;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 48 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 49 | `    plannerConfig.blendEnabled = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 50 | `    plannerConfig.blendDistanceMm = 1.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 51 | `    plannerConfig.blendSegmentLengthMm = 0.5f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 52 | `    plannerConfig.maxBlendSegments = 8;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 53 | `    plannerConfig.maxTrimRatio = 0.35f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 54 | `    plannerConfig.minMovementMm = 0.01f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 55 | `    plannerConfig.blendSpeedScale = 0.70f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 56 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 57 | `    if (validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 58 | `        planner.configure(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 59 | `            axisCount,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 60 | `            plannerConfig` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 61 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 62 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 63 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 64 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 65 | `bool AdvanceMotionController::loadAxes(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 66 | `    AdvanceStepperAxis* axisList[],` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 67 | `    uint numberOfAxes` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 68 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 69 | `    if (axisList == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 70 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 71 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 72 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 73 | `    if (numberOfAxes < MIN_AXES) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 74 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 75 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 76 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 77 | `    if (numberOfAxes > MAX_AXES) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 78 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 79 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 80 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 81 | `    for (uint i = 0; i < numberOfAxes; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 82 | `        if (axisList[i] == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 83 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 84 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 85 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 86 | `        axes[i] = axisList[i];` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 87 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 88 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 89 | `    axisCount = numberOfAxes;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 90 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 91 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 92 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 93 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 94 | `bool AdvanceMotionController::validateAxesStepPins() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 95 | `    if (pulseEngine == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 96 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 97 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 98 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 99 | `    bool usedOffsets[MAX_AXES];` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 100 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 101 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 102 | `        usedOffsets[i] = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 103 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 104 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 105 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 106 | `        uint stepPin =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 107 | `            axes[i]->getStepPin();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 108 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 109 | `        if (!pulseEngine->pinBelongsToStepGroup(stepPin)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 110 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 111 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 112 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 113 | `        uint offset =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 114 | `            pulseEngine->getPinOffset(stepPin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 115 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 116 | `        if (offset >= MAX_AXES) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 117 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 118 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 119 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 120 | `        if (usedOffsets[offset]) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 121 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 122 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 123 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 124 | `        usedOffsets[offset] = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 125 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 126 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 127 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 128 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 129 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 130 | `bool AdvanceMotionController::textEquals(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 131 | `    const char* firstText,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 132 | `    const char* secondText` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 133 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 134 | `    if (firstText == nullptr \|\| secondText == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 135 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 136 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 137 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 138 | `    uint index = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 139 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 140 | `    while (` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 141 | `        firstText[index] != '\0' \|\|` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 142 | `        secondText[index] != '\0'` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 143 | `    ) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 144 | `        if (firstText[index] != secondText[index]) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 145 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 146 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 147 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 148 | `        index++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 149 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 150 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 151 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 152 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 153 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 154 | `bool AdvanceMotionController::isReservedAxisName(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 155 | `    const char* axisName` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 156 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 157 | `    if (axisName == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 158 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 159 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 160 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 161 | `    if (axisName[0] == '\0') {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 162 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 163 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 164 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 165 | `    if (textEquals(axisName, "UNNAMED")) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 166 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 167 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 168 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 169 | `    return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 170 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 171 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 172 | `bool AdvanceMotionController::validateAxisNames() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 173 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 174 | `        const char* currentName =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 175 | `            axes[i]->getAxisName();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 176 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 177 | `        if (isReservedAxisName(currentName)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 178 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 179 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 180 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 181 | `        for (uint j = i + 1; j < axisCount; j++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 182 | `            const char* compareName =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 183 | `                axes[j]->getAxisName();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 184 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 185 | `            if (textEquals(currentName, compareName)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 186 | `                return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 187 | `            }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 188 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 189 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 190 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 191 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 192 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 193 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 194 | `bool AdvanceMotionController::normalizeAxisName(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 195 | `    const char* inputName,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 196 | `    char outputName[],` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 197 | `    uint outputSize` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 198 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 199 | `    if (inputName == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 200 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 201 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 202 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 203 | `    if (outputName == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 204 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 205 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 206 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 207 | `    if (outputSize == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 208 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 209 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 210 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 211 | `    uint writeIndex = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 212 | `    bool hasValidCharacter = false;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 213 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 214 | `    for (` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 215 | `        uint readIndex = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 216 | `        inputName[readIndex] != '\0' &&` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 217 | `        writeIndex < (outputSize - 1);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 218 | `        readIndex++` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 219 | `    ) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 220 | `        char currentChar =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 221 | `            inputName[readIndex];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 222 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 223 | `        if (` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 224 | `            currentChar == ' ' \|\|` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 225 | `            currentChar == '\t' \|\|` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 226 | `            currentChar == '\r' \|\|` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 227 | `            currentChar == '\n'` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 228 | `        ) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 229 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 230 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 231 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 232 | `        if (currentChar >= 'a' && currentChar <= 'z') {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 233 | `            currentChar =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 234 | `                currentChar - ('a' - 'A');` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 235 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 236 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 237 | `        outputName[writeIndex] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 238 | `            currentChar;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 239 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 240 | `        hasValidCharacter = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 241 | `        writeIndex++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 242 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 243 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 244 | `    outputName[writeIndex] = '\0';` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 245 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 246 | `    return hasValidCharacter;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 247 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 248 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 249 | `bool AdvanceMotionController::findAxisIndexByName(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 250 | `    const char* axisName,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 251 | `    uint& outputAxisIndex` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 252 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 253 | `    char normalizedName[StepperAxisConfig::AXIS_NAME_MAX_LENGTH];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 254 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 255 | `    if (!normalizeAxisName(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 256 | `            axisName,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 257 | `            normalizedName,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 258 | `            StepperAxisConfig::AXIS_NAME_MAX_LENGTH` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 259 | `        )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 260 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 261 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 262 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 263 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 264 | `        if (textEquals(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 265 | `                axes[i]->getAxisName(),` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 266 | `                normalizedName` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 267 | `            )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 268 | `            outputAxisIndex = i;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 269 | `            return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 270 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 271 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 272 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 273 | `    return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 274 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 275 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 276 | `void AdvanceMotionController::init() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 277 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 278 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 279 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 280 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 281 | `    if (pulseEngine != nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 282 | `        pulseEngine->init();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 283 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 284 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 285 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 286 | `        axes[i]->init();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 287 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 288 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 289 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 290 | `bool AdvanceMotionController::isValid() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 291 | `    return validController;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 292 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 293 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 294 | `uint AdvanceMotionController::getAxisCount() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 295 | `    return axisCount;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 296 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 297 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 298 | `const char* AdvanceMotionController::getAxisName(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 299 | `    uint axisIndex` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 300 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 301 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 302 | `        return "INVALID";` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 303 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 304 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 305 | `    if (axes[axisIndex] == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 306 | `        return "NULL";` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 307 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 308 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 309 | `    return axes[axisIndex]->getAxisName();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 310 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 311 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 312 | `void AdvanceMotionController::printAxisMap() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 313 | `    printf("\n===== AXIS MAP =====\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 314 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 315 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 316 | `        printf("Controller invalid.\n");` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 317 | `        printf("====================\n\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 318 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 319 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 320 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 321 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 322 | `        printf(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 323 | `            "Index [%u] = Axis %s \| STEP GPIO %u \| DIR GPIO %u \| ENABLE GPIO %u\n",` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 324 | `            i,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 325 | `            axes[i]->getAxisName(),` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 326 | `            axes[i]->getStepPin(),` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 327 | `            axes[i]->getDirPin(),` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 328 | `            axes[i]->getEnablePin()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 329 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 330 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 331 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 332 | `    printf("====================\n\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 333 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 334 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 335 | `void AdvanceMotionController::printHomeOrder() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 336 | `    printf("\n===== HOME ORDER =====\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 337 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 338 | `    if (!homeOrderConfigured) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 339 | `        printf("Home order not configured.\n");` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 340 | `        printf("======================\n\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 341 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 342 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 343 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 344 | `    for (uint i = 0; i < homeOrderCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 345 | `        uint axisIndex =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 346 | `            homeOrder[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 347 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 348 | `        printf(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 349 | `            "Home step %u: Axis index [%u] = %s\n",` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 350 | `            i + 1,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 351 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 352 | `            getAxisName(axisIndex)` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 353 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 354 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 355 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 356 | `    printf("======================\n\n");` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 357 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 358 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 359 | `float AdvanceMotionController::calculateDistanceFromDeltas(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 360 | `    const float deltas[],` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 361 | `    uint count` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 362 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 363 | `    float sum = 0.0f;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 364 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 365 | `    for (uint i = 0; i < count; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 366 | `        sum += deltas[i] * deltas[i];` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 367 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 368 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 369 | `    return sqrtf(sum);` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 370 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 371 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 372 | `bool AdvanceMotionController::buildLinearBlockToArray(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 373 | `    const float targetPositionsMm[],` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 374 | `    uint targetCount,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 375 | `    float requestedSpeedMmS,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 376 | `    float entrySpeedMmS,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 377 | `    float exitSpeedMmS,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 378 | `    MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 379 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 380 | `    block.reset();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 381 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 382 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 383 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 384 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 385 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 386 | `    if (targetPositionsMm == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 387 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 388 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 389 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 390 | `    if (targetCount != axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 391 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 392 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 393 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 394 | `    if (requestedSpeedMmS <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 395 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 396 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 397 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 398 | `    block.axisCount = axisCount;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 399 | `    block.requestedSpeedMmS = requestedSpeedMmS;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 400 | `    block.entrySpeedMmS = entrySpeedMmS;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 401 | `    block.exitSpeedMmS = exitSpeedMmS;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 402 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 403 | `    uint32_t dominantSteps = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 404 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 405 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 406 | `        float currentMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 407 | `            axes[i]->getCurrentPositionMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 408 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 409 | `        float targetMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 410 | `            targetPositionsMm[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 411 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 412 | `        if (!axes[i]->isTargetWithinSoftLimits(targetMm)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 413 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 414 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 415 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 416 | `        block.deltaMm[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 417 | `            targetMm - currentMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 418 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 419 | `        int32_t targetSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 420 | `            axes[i]->mmToSteps(targetMm);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 421 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 422 | `        int32_t currentSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 423 | `            axes[i]->getCurrentPositionSteps();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 424 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 425 | `        block.deltaSteps[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 426 | `            targetSteps - currentSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 427 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 428 | `        block.direction[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 429 | `            block.deltaSteps[i] >= 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 430 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 431 | `        if (block.deltaSteps[i] >= 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 432 | `            block.absSteps[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 433 | `                (uint32_t)block.deltaSteps[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 434 | `        } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 435 | `            block.absSteps[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 436 | `                (uint32_t)(-block.deltaSteps[i]);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 437 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 438 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 439 | `        if (block.absSteps[i] > dominantSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 440 | `            dominantSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 441 | `                block.absSteps[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 442 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 443 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 444 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 445 | `    block.dominantSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 446 | `        dominantSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 447 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 448 | `    if (block.dominantSteps == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 449 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 450 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 451 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 452 | `    block.distanceMm =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 453 | `        calculateDistanceFromDeltas(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 454 | `            block.deltaMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 455 | `            axisCount` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 456 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 457 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 458 | `    if (block.distanceMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 459 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 460 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 461 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 462 | `    float limitedSpeed =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 463 | `        limitSpeedByAxisLimits(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 464 | `            block,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 465 | `            requestedSpeedMmS` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 466 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 467 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 468 | `    limitedSpeed =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 469 | `        limitSpeedByPulseEngine(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 470 | `            block,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 471 | `            limitedSpeed` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 472 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 473 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 474 | `    block.nominalSpeedMmS =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 475 | `        limitedSpeed;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 476 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 477 | `    block.accelerationMmS2 =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 478 | `        limitAccelerationByAxisLimits(block);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 479 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 480 | `    if (block.nominalSpeedMmS <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 481 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 482 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 483 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 484 | `    if (block.accelerationMmS2 <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 485 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 486 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 487 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 488 | `    block.valid = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 489 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 490 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 491 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 492 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 493 | `bool AdvanceMotionController::buildLinearBlockTo(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 494 | `    std::initializer_list<float> targetPositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 495 | `    float requestedSpeedMmS,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 496 | `    MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 497 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 498 | `    if (targetPositionsMm.size() != axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 499 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 500 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 501 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 502 | `    float targetArray[MAX_AXES];` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 503 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 504 | `    uint index = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 505 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 506 | `    for (float value : targetPositionsMm) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 507 | `        targetArray[index] = value;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 508 | `        index++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 509 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 510 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 511 | `    return buildLinearBlockToArray(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 512 | `        targetArray,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 513 | `        axisCount,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 514 | `        requestedSpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 515 | `        0.0f,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 516 | `        0.0f,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 517 | `        block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 518 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 519 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 520 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 521 | `float AdvanceMotionController::limitSpeedByAxisLimits(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 522 | `    const MotionBlock& block,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 523 | `    float requestedSpeedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 524 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 525 | `    float limitedSpeed =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 526 | `        requestedSpeedMmS;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 527 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 528 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 529 | `        if (block.absSteps[i] == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 530 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 531 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 532 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 533 | `        float axisDistanceMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 534 | `            fabsf(block.deltaMm[i]);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 535 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 536 | `        if (axisDistanceMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 537 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 538 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 539 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 540 | `        float axisRatio =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 541 | `            axisDistanceMm /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 542 | `            block.distanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 543 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 544 | `        if (axisRatio <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 545 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 546 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 547 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 548 | `        float maxPathSpeedByAxis =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 549 | `            axes[i]->getMaxSpeedMmS() /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 550 | `            axisRatio;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 551 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 552 | `        if (maxPathSpeedByAxis < limitedSpeed) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 553 | `            limitedSpeed =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 554 | `                maxPathSpeedByAxis;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 555 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 556 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 557 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 558 | `    return limitedSpeed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 559 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 560 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 561 | `float AdvanceMotionController::limitSpeedByPulseEngine(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 562 | `    const MotionBlock& block,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 563 | `    float currentLimitedSpeed` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 564 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 565 | `    if (pulseEngine == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 566 | `        return currentLimitedSpeed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 567 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 568 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 569 | `    if (block.distanceMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 570 | `        return currentLimitedSpeed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 571 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 572 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 573 | `    if (block.dominantSteps == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 574 | `        return currentLimitedSpeed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 575 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 576 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 577 | `    float maxEventsPerSecond =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 578 | `        pulseEngine->getMaxStepEventsPerSecond();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 579 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 580 | `    float maxPathSpeedByPulse =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 581 | `        (` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 582 | `            maxEventsPerSecond *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 583 | `            block.distanceMm` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 584 | `        ) /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 585 | `        (float)block.dominantSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 586 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 587 | `    if (maxPathSpeedByPulse < currentLimitedSpeed) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 588 | `        return maxPathSpeedByPulse;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 589 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 590 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 591 | `    return currentLimitedSpeed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 592 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 593 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 594 | `float AdvanceMotionController::limitAccelerationByAxisLimits(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 595 | `    const MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 596 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 597 | `    float limitedAcceleration =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 598 | `        1000000.0f;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 599 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 600 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 601 | `        if (block.absSteps[i] == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 602 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 603 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 604 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 605 | `        float axisDistanceMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 606 | `            fabsf(block.deltaMm[i]);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 607 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 608 | `        if (axisDistanceMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 609 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 610 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 611 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 612 | `        float axisRatio =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 613 | `            axisDistanceMm /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 614 | `            block.distanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 615 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 616 | `        if (axisRatio <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 617 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 618 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 619 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 620 | `        float maxPathAccelerationByAxis =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 621 | `            axes[i]->getAccelerationMmS2() /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 622 | `            axisRatio;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 623 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 624 | `        if (maxPathAccelerationByAxis < limitedAcceleration) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 625 | `            limitedAcceleration =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 626 | `                maxPathAccelerationByAxis;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 627 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 628 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 629 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 630 | `    if (limitedAcceleration == 1000000.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 631 | `        limitedAcceleration = 1.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 632 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 633 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 634 | `    return limitedAcceleration;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 635 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 636 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 637 | `void AdvanceMotionController::prepareDirections(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 638 | `    const MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 639 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 640 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 641 | `        if (block.absSteps[i] == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 642 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 643 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 644 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 645 | `        axes[i]->setDirection(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 646 | `            block.direction[i]` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 647 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 648 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 649 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 650 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 651 | `void AdvanceMotionController::prepareBresenham(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 652 | `    const MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 653 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 654 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 655 | `        bresenhamError[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 656 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 657 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 658 | `    dominantStepCounter = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 659 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 660 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 661 | `bool AdvanceMotionController::startMotionBlock(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 662 | `    const MotionBlock& block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 663 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 664 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 665 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 666 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 667 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 668 | `    if (!block.valid) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 669 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 670 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 671 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 672 | `    if (busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 673 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 674 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 675 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 676 | `    currentBlock =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 677 | `        block;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 678 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 679 | `    currentProfile =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 680 | `        TrapezoidProfile::calculate(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 681 | `            currentBlock.dominantSteps,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 682 | `            currentBlock.distanceMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 683 | `            currentBlock.nominalSpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 684 | `            currentBlock.accelerationMmS2,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 685 | `            currentBlock.entrySpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 686 | `            currentBlock.exitSpeedMmS` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 687 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 688 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 689 | `    if (!currentProfile.valid) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 690 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 691 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 692 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 693 | `    prepareDirections(currentBlock);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 694 | `    prepareBresenham(currentBlock);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 695 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 696 | `    nextStepTime =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 697 | `        get_absolute_time();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 698 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 699 | `    busy = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 700 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 701 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 702 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 703 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 704 | `bool AdvanceMotionController::startPlannedLinearMove(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 705 | `    const PlannedLinearMove& plannedMove` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 706 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 707 | `    if (!plannedMove.valid) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 708 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 709 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 710 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 711 | `    MotionBlock block;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 712 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 713 | `    if (!buildLinearBlockToArray(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 714 | `            plannedMove.targetPositionMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 715 | `            plannedMove.axisCount,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 716 | `            plannedMove.requestedSpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 717 | `            plannedMove.entrySpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 718 | `            plannedMove.exitSpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 719 | `            block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 720 | `        )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 721 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 722 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 723 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 724 | `    return startMotionBlock(block);` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 725 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 726 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 727 | `bool AdvanceMotionController::moveLinearTo(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 728 | `    std::initializer_list<float> targetPositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 729 | `    float requestedSpeedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 730 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 731 | `    if (busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 732 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 733 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 734 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 735 | `    MotionBlock block;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 736 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 737 | `    if (!buildLinearBlockTo(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 738 | `            targetPositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 739 | `            requestedSpeedMmS,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 740 | `            block` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 741 | `        )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 742 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 743 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 744 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 745 | `    return startMotionBlock(block);` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 746 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 747 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 748 | `void AdvanceMotionController::configurePathSmoothing(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 749 | `    bool enabled,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 750 | `    float blendDistanceMm,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 751 | `    float blendSegmentLengthMm,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 752 | `    uint maxBlendSegments,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 753 | `    float blendSpeedScale` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 754 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 755 | `    planner.setBlendEnabled(enabled);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 756 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 757 | `    planner.setBlendParameters(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 758 | `        blendDistanceMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 759 | `        blendSegmentLengthMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 760 | `        maxBlendSegments,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 761 | `        blendSpeedScale` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 762 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 763 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 764 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 765 | `void AdvanceMotionController::clearMotionQueue() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 766 | `    planner.clear();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 767 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 768 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 769 | `bool AdvanceMotionController::queueLinearTo(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 770 | `    std::initializer_list<float> targetPositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 771 | `    float requestedSpeedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 772 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 773 | `    return planner.pushLinearTo(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 774 | `        targetPositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 775 | `        requestedSpeedMmS` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 776 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 777 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 778 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 779 | `bool AdvanceMotionController::prepareQueuedMoves() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 780 | `    float currentPositions[MAX_AXES];` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 781 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 782 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 783 | `        currentPositions[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 784 | `            axes[i]->getCurrentPositionMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 785 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 786 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 787 | `    planner.setStartPosition(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 788 | `        currentPositions,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 789 | `        axisCount` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 790 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 791 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 792 | `    return planner.buildPlannedQueue();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 793 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 794 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 795 | `uint AdvanceMotionController::getRawQueuedMoveCount() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 796 | `    return planner.getRawMoveCount();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 797 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 798 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 799 | `uint AdvanceMotionController::getPlannedMoveCount() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 800 | `    return planner.getPlannedMoveCount();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 801 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 802 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 803 | `void AdvanceMotionController::loadNextPlannedMoveIfAvailable() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 804 | `    if (busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 805 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 806 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 807 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 808 | `    PlannedLinearMove plannedMove;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 809 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 810 | `    if (!planner.popPlannedMove(plannedMove)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 811 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 812 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 813 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 814 | `    startPlannedLinearMove(plannedMove);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 815 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 816 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 817 | `uint32_t AdvanceMotionController::calculateCurrentStepDelayUs() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 818 | `    if (currentBlock.dominantSteps == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 819 | `        return pulseEngine->getMinimumStepIntervalUs();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 820 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 821 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 822 | `    if (currentBlock.distanceMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 823 | `        return pulseEngine->getMinimumStepIntervalUs();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 824 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 825 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 826 | `    float distancePerDominantStep =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 827 | `        currentBlock.distanceMm /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 828 | `        (float)currentBlock.dominantSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 829 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 830 | `    float traveledDistanceMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 831 | `        distancePerDominantStep *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 832 | `        (float)dominantStepCounter;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 833 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 834 | `    float speedMmS =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 835 | `        currentProfile.reachableSpeedMmS;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 836 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 837 | `    float entrySquared =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 838 | `        currentProfile.entrySpeedMmS *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 839 | `        currentProfile.entrySpeedMmS;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 840 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 841 | `    float exitSquared =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 842 | `        currentProfile.exitSpeedMmS *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 843 | `        currentProfile.exitSpeedMmS;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 844 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 845 | `    if (dominantStepCounter < currentProfile.accelerationSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 846 | `        float speedSquared =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 847 | `            entrySquared +` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 848 | `            2.0f *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 849 | `            currentProfile.accelerationMmS2 *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 850 | `            traveledDistanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 851 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 852 | `        speedMmS =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 853 | `            sqrtf(speedSquared);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 854 | `    } else if (` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 855 | `        dominantStepCounter >=` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 856 | `        (` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 857 | `            currentProfile.accelerationSteps +` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 858 | `            currentProfile.cruiseSteps` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 859 | `        )` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 860 | `    ) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 861 | `        float remainingDistanceMm =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 862 | `            currentBlock.distanceMm -` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 863 | `            traveledDistanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 864 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 865 | `        if (remainingDistanceMm < 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 866 | `            remainingDistanceMm = 0.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 867 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 868 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 869 | `        float speedSquared =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 870 | `            exitSquared +` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 871 | `            2.0f *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 872 | `            currentProfile.accelerationMmS2 *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 873 | `            remainingDistanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 874 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 875 | `        speedMmS =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 876 | `            sqrtf(speedSquared);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 877 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 878 | `        speedMmS =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 879 | `            currentProfile.reachableSpeedMmS;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 880 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 881 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 882 | `    if (speedMmS < 0.1f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 883 | `        speedMmS = 0.1f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 884 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 885 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 886 | `    float stepEventsPerSecond =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 887 | `        (` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 888 | `            speedMmS *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 889 | `            (float)currentBlock.dominantSteps` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 890 | `        ) /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 891 | `        currentBlock.distanceMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 892 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 893 | `    if (stepEventsPerSecond <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 894 | `        return pulseEngine->getMinimumStepIntervalUs();` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 895 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 896 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 897 | `    uint32_t delayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 898 | `        (uint32_t)(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 899 | `            1000000.0f /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 900 | `            stepEventsPerSecond` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 901 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 902 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 903 | `    if (delayUs < pulseEngine->getMinimumStepIntervalUs()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 904 | `        delayUs =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 905 | `            pulseEngine->getMinimumStepIntervalUs();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 906 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 907 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 908 | `    return delayUs;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 909 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 910 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 911 | `bool AdvanceMotionController::isHardwareLimitBlockingDirection(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 912 | `    uint axisIndex,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 913 | `    bool positiveDirection` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 914 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 915 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 916 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 917 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 918 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 919 | `    AdvanceStepperAxis* axis =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 920 | `        axes[axisIndex];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 921 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 922 | `    if (positiveDirection) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 923 | `        if (axis->isMaxLimitEnabled() && axis->isMaxLimitActive()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 924 | `            return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 925 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 926 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 927 | `        if (axis->isMinLimitEnabled() && axis->isMinLimitActive()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 928 | `            return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 929 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 930 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 931 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 932 | `    return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 933 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 934 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 935 | `void AdvanceMotionController::executeBresenhamStep() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 936 | `    if (!busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 937 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 938 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 939 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 940 | `    if (dominantStepCounter >= currentBlock.dominantSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 941 | `        busy = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 942 | `        currentBlock.reset();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 943 | `        loadNextPlannedMoveIfAvailable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 944 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 945 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 946 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 947 | `    uint32_t stepMask = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 948 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 949 | `    int32_t positionDelta[MAX_AXES];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 950 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 951 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 952 | `        positionDelta[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 953 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 954 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 955 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 956 | `        if (currentBlock.absSteps[i] == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 957 | `            continue;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 958 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 959 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 960 | `        bresenhamError[i] +=` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 961 | `            (int32_t)currentBlock.absSteps[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 962 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 963 | `        if (bresenhamError[i] >= (int32_t)currentBlock.dominantSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 964 | `            bresenhamError[i] -=` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 965 | `                (int32_t)currentBlock.dominantSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 966 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 967 | `            bool positiveDirection =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 968 | `                currentBlock.direction[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 969 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 970 | `            if (isHardwareLimitBlockingDirection(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 971 | `                    i,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 972 | `                    positiveDirection` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 973 | `                )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 974 | `                emergencyStop();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 975 | `                return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 976 | `            }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 977 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 978 | `            uint stepPin =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 979 | `                axes[i]->getStepPin();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 980 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 981 | `            uint pinOffset =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 982 | `                pulseEngine->getPinOffset(stepPin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 983 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 984 | `            stepMask \|=` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 985 | `                (1u << pinOffset);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 986 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 987 | `            if (positiveDirection) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 988 | `                positionDelta[i] = 1;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 989 | `            } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 990 | `                positionDelta[i] = -1;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 991 | `            }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 992 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 993 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 994 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 995 | `    if (stepMask != 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 996 | `        pulseEngine->sendStepMaskBlocking(stepMask);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 997 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 998 | `        for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 999 | `            if (positionDelta[i] != 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1000 | `                axes[i]->addPositionSteps(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1001 | `                    positionDelta[i]` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1002 | `                );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1003 | `            }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1004 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1005 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1006 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1007 | `    dominantStepCounter++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1008 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1009 | `    if (dominantStepCounter >= currentBlock.dominantSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1010 | `        busy = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1011 | `        currentBlock.reset();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1012 | `        loadNextPlannedMoveIfAvailable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1013 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1014 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1015 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1016 | `void AdvanceMotionController::update() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1017 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1018 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1019 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1020 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1021 | `    if (!busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1022 | `        loadNextPlannedMoveIfAvailable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1023 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1024 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1025 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1026 | `    absolute_time_t now =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1027 | `        get_absolute_time();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1028 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1029 | `    if (absolute_time_diff_us(now, nextStepTime) <= 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1030 | `        executeBresenhamStep();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1031 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1032 | `        uint32_t delayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1033 | `            calculateCurrentStepDelayUs();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1034 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1035 | `        nextStepTime =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1036 | `            delayed_by_us(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1037 | `                nextStepTime,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1038 | `                delayUs` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1039 | `            );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1040 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1041 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1042 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1043 | `bool AdvanceMotionController::isBusy() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1044 | `    return busy;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1045 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1046 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1047 | `void AdvanceMotionController::stop() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1048 | `    busy = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1049 | `    currentBlock.reset();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1050 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1051 | `    planner.clear();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1052 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1053 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1054 | `void AdvanceMotionController::emergencyStop() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1055 | `    stop();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1056 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1057 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1058 | `        axes[i]->disable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1059 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1060 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1061 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1062 | `void AdvanceMotionController::setCurrentPosition(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1063 | `    std::initializer_list<float> currentPositionsMm` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1064 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1065 | `    if (currentPositionsMm.size() != axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1066 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1067 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1068 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1069 | `    uint index = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1070 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1071 | `    for (float positionMm : currentPositionsMm) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1072 | `        axes[index]->setCurrentPositionMm(positionMm);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1073 | `        index++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1074 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1075 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1076 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1077 | `bool AdvanceMotionController::getCurrentPosition(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1078 | `    float outputPositionsMm[],` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1079 | `    uint outputCount` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1080 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1081 | `    if (outputPositionsMm == nullptr) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1082 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1083 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1084 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1085 | `    if (outputCount != axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1086 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1087 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1088 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1089 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1090 | `        outputPositionsMm[i] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1091 | `            axes[i]->getCurrentPositionMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1092 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1093 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1094 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1095 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1096 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1097 | `uint32_t AdvanceMotionController::speedToStepDelayUs(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1098 | `    uint axisIndex,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1099 | `    float speedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1100 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1101 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1102 | `        return 1000;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1103 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1104 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1105 | `    if (speedMmS <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1106 | `        return 1000;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1107 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1108 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1109 | `    float stepsPerSecond =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1110 | `        speedMmS *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1111 | `        axes[axisIndex]->getStepsPerMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1112 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1113 | `    if (stepsPerSecond <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1114 | `        return 1000;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1115 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1116 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1117 | `    uint32_t delayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1118 | `        (uint32_t)(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1119 | `            1000000.0f /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1120 | `            stepsPerSecond` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1121 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1122 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1123 | `    if (delayUs < pulseEngine->getMinimumStepIntervalUs()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1124 | `        delayUs =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1125 | `            pulseEngine->getMinimumStepIntervalUs();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1126 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1127 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1128 | `    return delayUs;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1129 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1130 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1131 | `bool AdvanceMotionController::stepAxisOnceBlocking(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1132 | `    uint axisIndex,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1133 | `    bool positiveDirection,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1134 | `    uint32_t delayUs` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1135 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1136 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1137 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1138 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1139 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1140 | `    if (isHardwareLimitBlockingDirection(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1141 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1142 | `            positiveDirection` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1143 | `        )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1144 | `        emergencyStop();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1145 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1146 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1147 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1148 | `    axes[axisIndex]->setDirection(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1149 | `        positiveDirection` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1150 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1151 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1152 | `    uint stepPin =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1153 | `        axes[axisIndex]->getStepPin();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1154 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1155 | `    uint pinOffset =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1156 | `        pulseEngine->getPinOffset(stepPin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1157 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1158 | `    uint32_t stepMask =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1159 | `        (1u << pinOffset);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1160 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1161 | `    pulseEngine->sendStepMaskBlocking(stepMask);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1162 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1163 | `    if (positiveDirection) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1164 | `        axes[axisIndex]->addPositionSteps(1);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1165 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1166 | `        axes[axisIndex]->addPositionSteps(-1);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1167 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1168 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1169 | `    sleep_us(delayUs);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1170 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1171 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1172 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1173 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1174 | `bool AdvanceMotionController::moveAxisRelativeBlocking(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1175 | `    uint axisIndex,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1176 | `    float distanceMm,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1177 | `    float speedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1178 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1179 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1180 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1181 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1182 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1183 | `    if (speedMmS <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1184 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1185 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1186 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1187 | `    int32_t stepsToMove =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1188 | `        axes[axisIndex]->mmToSteps(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1189 | `            fabsf(distanceMm)` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1190 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1191 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1192 | `    if (stepsToMove <= 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1193 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1194 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1195 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1196 | `    bool positiveDirection =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1197 | `        distanceMm >= 0.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1198 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1199 | `    uint32_t delayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1200 | `        speedToStepDelayUs(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1201 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1202 | `            speedMmS` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1203 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1204 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1205 | `    for (int32_t step = 0; step < stepsToMove; step++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1206 | `        if (!stepAxisOnceBlocking(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1207 | `                axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1208 | `                positiveDirection,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1209 | `                delayUs` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1210 | `            )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1211 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1212 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1213 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1214 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1215 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1216 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1217 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1218 | `bool AdvanceMotionController::setHomeOrder(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1219 | `    std::initializer_list<uint> orderedAxisIndexes` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1220 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1221 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1222 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1223 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1224 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1225 | `    if (orderedAxisIndexes.size() == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1226 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1227 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1228 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1229 | `    if (orderedAxisIndexes.size() > axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1230 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1231 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1232 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1233 | `    bool axisAlreadyAdded[MAX_AXES];` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1234 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1235 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1236 | `        axisAlreadyAdded[i] = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1237 | `        homeOrder[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1238 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1239 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1240 | `    uint index = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1241 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1242 | `    for (uint axisIndex : orderedAxisIndexes) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1243 | `        if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1244 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1245 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1246 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1247 | `        if (axisAlreadyAdded[axisIndex]) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1248 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1249 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1250 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1251 | `        homeOrder[index] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1252 | `            axisIndex;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1253 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1254 | `        axisAlreadyAdded[axisIndex] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1255 | `            true;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1256 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1257 | `        index++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1258 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1259 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1260 | `    homeOrderCount = index;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1261 | `    homeOrderConfigured = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1262 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1263 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1264 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1265 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1266 | `bool AdvanceMotionController::setHomeOrderByName(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1267 | `    std::initializer_list<const char*> orderedAxisNames` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1268 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1269 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1270 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1271 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1272 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1273 | `    if (orderedAxisNames.size() == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1274 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1275 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1276 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1277 | `    if (orderedAxisNames.size() > axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1278 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1279 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1280 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1281 | `    bool axisAlreadyAdded[MAX_AXES];` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1282 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1283 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1284 | `        axisAlreadyAdded[i] = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1285 | `        homeOrder[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1286 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1287 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1288 | `    uint orderIndex = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1289 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1290 | `    for (const char* axisName : orderedAxisNames) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1291 | `        uint foundAxisIndex = 0;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1292 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1293 | `        if (!findAxisIndexByName(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1294 | `                axisName,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1295 | `                foundAxisIndex` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1296 | `            )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1297 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1298 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1299 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1300 | `        if (axisAlreadyAdded[foundAxisIndex]) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1301 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1302 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1303 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1304 | `        homeOrder[orderIndex] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1305 | `            foundAxisIndex;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1306 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1307 | `        axisAlreadyAdded[foundAxisIndex] =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1308 | `            true;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1309 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1310 | `        orderIndex++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1311 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1312 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1313 | `    homeOrderCount = orderIndex;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1314 | `    homeOrderConfigured = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1315 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1316 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1317 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1318 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1319 | `void AdvanceMotionController::clearHomeOrder() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1320 | `    for (uint i = 0; i < MAX_AXES; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1321 | `        homeOrder[i] = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1322 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1323 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1324 | `    homeOrderCount = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1325 | `    homeOrderConfigured = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1326 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1327 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1328 | `bool AdvanceMotionController::homeAxis(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1329 | `    uint axisIndex` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1330 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1331 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1332 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1333 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1334 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1335 | `    if (busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1336 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1337 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1338 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1339 | `    if (axisIndex >= axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1340 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1341 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1342 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1343 | `    AdvanceStepperAxis* axis =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1344 | `        axes[axisIndex];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1345 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1346 | `    if (!axis->isHomeEnabled()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1347 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1348 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1349 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1350 | `    axis->enable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1351 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1352 | `    if (` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1353 | `        axis->isHomeSimulationEnabled() &&` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1354 | `        axis->isHomeSensorActive()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1355 | `    ) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1356 | `        axis->setCurrentPositionMm(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1357 | `            axis->getHomePositionMm()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1358 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1359 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1360 | `        axis->setHomed(true);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1361 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1362 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1363 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1364 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1365 | `    bool homeDirectionPositive =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1366 | `        axis->getHomeDirectionPositive();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1367 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1368 | `    uint32_t seekDelayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1369 | `        speedToStepDelayUs(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1370 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1371 | `            axis->getHomingSeekSpeedMmS()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1372 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1373 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1374 | `    uint32_t latchDelayUs =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1375 | `        speedToStepDelayUs(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1376 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1377 | `            axis->getHomingLatchSpeedMmS()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1378 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1379 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1380 | `    int32_t maxTravelSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1381 | `        axis->mmToSteps(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1382 | `            axis->getMaxHomingTravelMm()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1383 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1384 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1385 | `    if (maxTravelSteps < 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1386 | `        maxTravelSteps =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1387 | `            -maxTravelSteps;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1388 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1389 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1390 | `    int32_t traveledSteps = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1391 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1392 | `    while (!axis->isHomeSensorActive()) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1393 | `        if (traveledSteps >= maxTravelSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1394 | `            emergencyStop();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1395 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1396 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1397 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1398 | `        if (!stepAxisOnceBlocking(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1399 | `                axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1400 | `                homeDirectionPositive,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1401 | `                seekDelayUs` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1402 | `            )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1403 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1404 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1405 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1406 | `        traveledSteps++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1407 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1408 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1409 | `    float backoffDistance =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1410 | `        axis->getHomingBackoffMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1411 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1412 | `    if (homeDirectionPositive) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1413 | `        backoffDistance =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1414 | `            -backoffDistance;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1415 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1416 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1417 | `    if (!moveAxisRelativeBlocking(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1418 | `            axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1419 | `            backoffDistance,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1420 | `            axis->getHomingSeekSpeedMmS()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1421 | `        )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1422 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1423 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1424 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1425 | `    sleep_ms(100);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1426 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1427 | `    traveledSteps = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 1428 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1429 | `    while (!axis->isHomeSensorActive()) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1430 | `        if (traveledSteps >= maxTravelSteps) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1431 | `            emergencyStop();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1432 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1433 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1434 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1435 | `        if (!stepAxisOnceBlocking(` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1436 | `                axisIndex,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1437 | `                homeDirectionPositive,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1438 | `                latchDelayUs` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1439 | `            )) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1440 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1441 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1442 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1443 | `        traveledSteps++;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1444 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1445 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1446 | `    axis->setCurrentPositionMm(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1447 | `        axis->getHomePositionMm()` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1448 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1449 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1450 | `    axis->setHomed(true);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1451 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1452 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1453 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1454 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1455 | `bool AdvanceMotionController::homeAll() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1456 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1457 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1458 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1459 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1460 | `    if (busy) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1461 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1462 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1463 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1464 | `    if (!homeOrderConfigured) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1465 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1466 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1467 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1468 | `    if (homeOrderCount == 0) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1469 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1470 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1471 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1472 | `    for (uint i = 0; i < homeOrderCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1473 | `        uint axisIndex =` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1474 | `            homeOrder[i];` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1475 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1476 | `        if (!homeAxis(axisIndex)) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1477 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1478 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1479 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1480 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1481 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1482 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1483 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1484 | `bool AdvanceMotionController::moveToSafePositionAfterHome(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1485 | `    std::initializer_list<float> safePositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1486 | `    float speedMmS` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 1487 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1488 | `    if (!validController) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1489 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1490 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1491 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1492 | `    if (safePositionsMm.size() != axisCount) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1493 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1494 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1495 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1496 | `    for (uint i = 0; i < axisCount; i++) {` | Inicia un ciclo de repeticion sobre indices, estados o condiciones. |
| 1497 | `        if (!axes[i]->isHomed()) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 1498 | `            return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1499 | `        }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1500 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1501 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 1502 | `    return moveLinearTo(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 1503 | `        safePositionsMm,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1504 | `        speedMmS` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1505 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 1506 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
