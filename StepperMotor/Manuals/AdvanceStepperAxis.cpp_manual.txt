MANUAL ACTUALIZADO - src/srcStepper/AdvanceStepperAxis.cpp
================================================================================
Funcion inicial del archivo
- Representa un eje individual: pines, conversion mm/pasos, direccion, HOME y limites.

Metodos, variables, secciones o instancias detectadas
- L5: `AdvanceStepperAxis::AdvanceStepperAxis(const StepperAxisConfig& axisConfig) {`
- L6: `config = axisConfig;`
- L8: `stepsPerMm = 0.0f;`
- L10: `currentPositionSteps = 0;`
- L11: `targetPositionSteps = 0;`
- L13: `enabled = false;`
- L14: `currentDirection = true;`
- L15: `homed = false;`
- L17: `simulatedHomeActive = false;`
- L19: `calculateStepsPerMm();`
- L22: `void AdvanceStepperAxis::calculateStepsPerMm() {`
- L23: `if (config.travelPerRevolutionMm <= 0.0f) {`
- L24: `stepsPerMm = 0.0f;`
- L28: `if (config.gearRatio <= 0.0f) {`
- L29: `stepsPerMm = 0.0f;`
- L33: `stepsPerMm =`
- L34: `((float)config.motorStepsPerRev *`
- L35: `(float)config.microsteps *`
- L44: `if (pin == StepperAxisConfig::PIN_NOT_USED) {`
- L48: `gpio_init(pin);`
- L49: `gpio_set_dir(pin, GPIO_IN);`
- L52: `gpio_pull_up(pin);`
- L54: `gpio_disable_pulls(pin);`
- L62: `if (pin == StepperAxisConfig::PIN_NOT_USED) {`
- L66: `bool rawState = gpio_get(pin);`
- L75: `void AdvanceStepperAxis::init() {`
- L76: `gpio_init(config.dirPin);`
- L77: `gpio_set_dir(config.dirPin, GPIO_OUT);`
- L78: `gpio_put(config.dirPin, 0);`
- L80: `gpio_init(config.enablePin);`
- L81: `gpio_set_dir(config.enablePin, GPIO_OUT);`
- L83: `disable();`
- L107: `const char* AdvanceStepperAxis::getAxisName() const {`
- L111: `void AdvanceStepperAxis::enable() {`
- L113: `gpio_put(config.enablePin, 0);`
- L115: `gpio_put(config.enablePin, 1);`
- L118: `enabled = true;`
- L121: `void AdvanceStepperAxis::disable() {`
- L123: `gpio_put(config.enablePin, 1);`
- L125: `gpio_put(config.enablePin, 0);`
- L128: `enabled = false;`
- L131: `void AdvanceStepperAxis::setDirection(bool positiveDirection) {`
- L132: `currentDirection = positiveDirection;`
- L134: `bool pinState = positiveDirection;`
- L137: `pinState = !pinState;`
- L140: `gpio_put(config.dirPin, pinState ? 1 : 0);`
- L143: `void AdvanceStepperAxis::setDirectionByDelta(int32_t deltaSteps) {`
- L144: `setDirection(deltaSteps >= 0);`
- L147: `void AdvanceStepperAxis::setCurrentPositionSteps(int32_t positionSteps) {`
- L148: `currentPositionSteps = positionSteps;`
- L151: `void AdvanceStepperAxis::setCurrentPositionMm(float positionMm) {`
- L152: `currentPositionSteps = mmToSteps(positionMm);`
- L155: `int32_t AdvanceStepperAxis::getCurrentPositionSteps() const {`
- L159: `float AdvanceStepperAxis::getCurrentPositionMm() const {`
- L160: `return stepsToMm(currentPositionSteps);`
- L163: `int32_t AdvanceStepperAxis::mmToSteps(float mm) const {`
- L164: `return (int32_t)lroundf(mm * stepsPerMm);`
- L167: `float AdvanceStepperAxis::stepsToMm(int32_t steps) const {`
- L168: `if (stepsPerMm <= 0.0f) {`
- L172: `return (float)steps / stepsPerMm;`
- L175: `float AdvanceStepperAxis::getStepsPerMm() const {`
- L179: `float AdvanceStepperAxis::getMaxSpeedMmS() const {`
- L183: `float AdvanceStepperAxis::getAccelerationMmS2() const {`
- L187: `uint AdvanceStepperAxis::getStepPin() const {`
- L191: `uint AdvanceStepperAxis::getDirPin() const {`
- L195: `uint AdvanceStepperAxis::getEnablePin() const {`
- L199: `bool AdvanceStepperAxis::isEnabled() const {`
- L203: `void AdvanceStepperAxis::addPositionSteps(int32_t deltaSteps) {`
- L204: `currentPositionSteps += deltaSteps;`
- L207: `bool AdvanceStepperAxis::isHomeEnabled() const {`
- L211: `bool AdvanceStepperAxis::isHomeSensorActive() const {`
- L226: `bool AdvanceStepperAxis::isMinLimitEnabled() const {`
- L230: `bool AdvanceStepperAxis::isMaxLimitEnabled() const {`
- L234: `bool AdvanceStepperAxis::isMinLimitActive() const {`
- L245: `bool AdvanceStepperAxis::isMaxLimitActive() const {`
- L256: `bool AdvanceStepperAxis::getHomeDirectionPositive() const {`
- L260: `float AdvanceStepperAxis::getHomingSeekSpeedMmS() const {`
- L264: `float AdvanceStepperAxis::getHomingLatchSpeedMmS() const {`
- L268: `float AdvanceStepperAxis::getHomingBackoffMm() const {`
- L272: `float AdvanceStepperAxis::getMaxHomingTravelMm() const {`
- L276: `float AdvanceStepperAxis::getHomePositionMm() const {`
- L280: `bool AdvanceStepperAxis::isHomeSimulationEnabled() const {`
- L284: `void AdvanceStepperAxis::setHomeSimulationEnabled(bool enabled) {`
- L285: `config.homeSimulationEnabled = enabled;`
- L288: `void AdvanceStepperAxis::setSimulatedHomeActive(bool active) {`
- L289: `simulatedHomeActive = active;`
- L292: `bool AdvanceStepperAxis::isHomed() const {`
- L296: `void AdvanceStepperAxis::setHomed(bool state) {`
- L297: `homed = state;`
- L300: `bool AdvanceStepperAxis::areSoftLimitsEnabled() const {`
- L304: `bool AdvanceStepperAxis::isTargetWithinSoftLimits(float targetMm) const {`
- L320: `float AdvanceStepperAxis::getSoftLimitMinMm() const {`
- L324: `float AdvanceStepperAxis::getSoftLimitMaxMm() const {`

Quien lo manda a llamar / relacion con otros archivos
- Los headers en lib son incluidos por implementaciones en src y por prgProject/main.cpp.
- Los .cpp son compilados por prgProject/CMakeLists.txt y sus funciones se ejecutan desde main.cpp o desde otros objetos del controlador.
- Los archivos config/recipes/*.txt son consumidos por RecipeLoader/SdRecipeStorage/MachineRecipeParser cuando se carga una receta.
- Los manuales son referencia de uso y no participan en la compilacion.

Explicacion linea por linea
| Linea | Codigo | Explicacion |
|---:|---|---|
| 1 | `#include "AdvanceStepperAxis.h"` | Incluye una dependencia necesaria para declaraciones, tipos o funciones usadas en este archivo. |
| 2 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 3 | `#include <math.h>` | Incluye una dependencia necesaria para declaraciones, tipos o funciones usadas en este archivo. |
| 4 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 5 | `AdvanceStepperAxis::AdvanceStepperAxis(const StepperAxisConfig& axisConfig) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 6 | `    config = axisConfig;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 7 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 8 | `    stepsPerMm = 0.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 9 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 10 | `    currentPositionSteps = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 11 | `    targetPositionSteps = 0;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 12 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 13 | `    enabled = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 14 | `    currentDirection = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 15 | `    homed = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 16 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 17 | `    simulatedHomeActive = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 18 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 19 | `    calculateStepsPerMm();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 20 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 21 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 22 | `void AdvanceStepperAxis::calculateStepsPerMm() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 23 | `    if (config.travelPerRevolutionMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 24 | `        stepsPerMm = 0.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 25 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 26 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 27 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 28 | `    if (config.gearRatio <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 29 | `        stepsPerMm = 0.0f;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 30 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 31 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 32 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 33 | `    stepsPerMm =` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 34 | `        ((float)config.motorStepsPerRev *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 35 | `         (float)config.microsteps *` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 36 | `         config.gearRatio) /` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 37 | `        config.travelPerRevolutionMm;` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 38 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 39 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 40 | `void AdvanceStepperAxis::configureInputPin(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 41 | `    uint pin,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 42 | `    bool usePullup` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 43 | `) {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 44 | `    if (pin == StepperAxisConfig::PIN_NOT_USED) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 45 | `        return;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 46 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 47 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 48 | `    gpio_init(pin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 49 | `    gpio_set_dir(pin, GPIO_IN);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 50 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 51 | `    if (usePullup) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 52 | `        gpio_pull_up(pin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 53 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 54 | `        gpio_disable_pulls(pin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 55 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 56 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 57 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 58 | `bool AdvanceStepperAxis::readInputActive(` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 59 | `    uint pin,` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 60 | `    bool activeLow` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 61 | `) const {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 62 | `    if (pin == StepperAxisConfig::PIN_NOT_USED) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 63 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 64 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 65 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 66 | `    bool rawState = gpio_get(pin);` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 67 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 68 | `    if (activeLow) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 69 | `        return !rawState;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 70 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 71 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 72 | `    return rawState;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 73 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 74 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 75 | `void AdvanceStepperAxis::init() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 76 | `    gpio_init(config.dirPin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 77 | `    gpio_set_dir(config.dirPin, GPIO_OUT);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 78 | `    gpio_put(config.dirPin, 0);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 79 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 80 | `    gpio_init(config.enablePin);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 81 | `    gpio_set_dir(config.enablePin, GPIO_OUT);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 82 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 83 | `    disable();` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 84 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 85 | `    if (config.homeEnabled && !config.homeSimulationEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 86 | `        configureInputPin(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 87 | `            config.homePin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 88 | `            config.homeUsePullup` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 89 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 90 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 91 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 92 | `    if (config.minLimitEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 93 | `        configureInputPin(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 94 | `            config.minLimitPin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 95 | `            config.minLimitUsePullup` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 96 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 97 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 98 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 99 | `    if (config.maxLimitEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 100 | `        configureInputPin(` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 101 | `            config.maxLimitPin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 102 | `            config.maxLimitUsePullup` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 103 | `        );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 104 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 105 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 106 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 107 | `const char* AdvanceStepperAxis::getAxisName() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 108 | `    return config.axisName;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 109 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 110 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 111 | `void AdvanceStepperAxis::enable() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 112 | `    if (config.enableActiveLow) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 113 | `        gpio_put(config.enablePin, 0);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 114 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 115 | `        gpio_put(config.enablePin, 1);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 116 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 117 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 118 | `    enabled = true;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 119 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 120 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 121 | `void AdvanceStepperAxis::disable() {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 122 | `    if (config.enableActiveLow) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 123 | `        gpio_put(config.enablePin, 1);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 124 | `    } else {` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 125 | `        gpio_put(config.enablePin, 0);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 126 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 127 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 128 | `    enabled = false;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 129 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 130 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 131 | `void AdvanceStepperAxis::setDirection(bool positiveDirection) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 132 | `    currentDirection = positiveDirection;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 133 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 134 | `    bool pinState = positiveDirection;` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 135 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 136 | `    if (config.directionInvert) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 137 | `        pinState = !pinState;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 138 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 139 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 140 | `    gpio_put(config.dirPin, pinState ? 1 : 0);` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 141 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 142 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 143 | `void AdvanceStepperAxis::setDirectionByDelta(int32_t deltaSteps) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 144 | `    setDirection(deltaSteps >= 0);` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 145 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 146 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 147 | `void AdvanceStepperAxis::setCurrentPositionSteps(int32_t positionSteps) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 148 | `    currentPositionSteps = positionSteps;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 149 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 150 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 151 | `void AdvanceStepperAxis::setCurrentPositionMm(float positionMm) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 152 | `    currentPositionSteps = mmToSteps(positionMm);` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 153 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 154 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 155 | `int32_t AdvanceStepperAxis::getCurrentPositionSteps() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 156 | `    return currentPositionSteps;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 157 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 158 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 159 | `float AdvanceStepperAxis::getCurrentPositionMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 160 | `    return stepsToMm(currentPositionSteps);` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 161 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 162 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 163 | `int32_t AdvanceStepperAxis::mmToSteps(float mm) const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 164 | `    return (int32_t)lroundf(mm * stepsPerMm);` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 165 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 166 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 167 | `float AdvanceStepperAxis::stepsToMm(int32_t steps) const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 168 | `    if (stepsPerMm <= 0.0f) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 169 | `        return 0.0f;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 170 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 171 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 172 | `    return (float)steps / stepsPerMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 173 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 174 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 175 | `float AdvanceStepperAxis::getStepsPerMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 176 | `    return stepsPerMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 177 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 178 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 179 | `float AdvanceStepperAxis::getMaxSpeedMmS() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 180 | `    return config.maxSpeedMmS;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 181 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 182 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 183 | `float AdvanceStepperAxis::getAccelerationMmS2() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 184 | `    return config.accelerationMmS2;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 185 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 186 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 187 | `uint AdvanceStepperAxis::getStepPin() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 188 | `    return config.stepPin;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 189 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 190 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 191 | `uint AdvanceStepperAxis::getDirPin() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 192 | `    return config.dirPin;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 193 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 194 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 195 | `uint AdvanceStepperAxis::getEnablePin() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 196 | `    return config.enablePin;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 197 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 198 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 199 | `bool AdvanceStepperAxis::isEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 200 | `    return enabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 201 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 202 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 203 | `void AdvanceStepperAxis::addPositionSteps(int32_t deltaSteps) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 204 | `    currentPositionSteps += deltaSteps;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 205 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 206 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 207 | `bool AdvanceStepperAxis::isHomeEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 208 | `    return config.homeEnabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 209 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 210 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 211 | `bool AdvanceStepperAxis::isHomeSensorActive() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 212 | `    if (!config.homeEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 213 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 214 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 215 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 216 | `    if (config.homeSimulationEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 217 | `        return simulatedHomeActive;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 218 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 219 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 220 | `    return readInputActive(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 221 | `        config.homePin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 222 | `        config.homeActiveLow` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 223 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 224 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 225 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 226 | `bool AdvanceStepperAxis::isMinLimitEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 227 | `    return config.minLimitEnabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 228 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 229 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 230 | `bool AdvanceStepperAxis::isMaxLimitEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 231 | `    return config.maxLimitEnabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 232 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 233 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 234 | `bool AdvanceStepperAxis::isMinLimitActive() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 235 | `    if (!config.minLimitEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 236 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 237 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 238 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 239 | `    return readInputActive(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 240 | `        config.minLimitPin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 241 | `        config.minLimitActiveLow` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 242 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 243 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 244 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 245 | `bool AdvanceStepperAxis::isMaxLimitActive() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 246 | `    if (!config.maxLimitEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 247 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 248 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 249 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 250 | `    return readInputActive(` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 251 | `        config.maxLimitPin,` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 252 | `        config.maxLimitActiveLow` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 253 | `    );` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 254 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 255 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 256 | `bool AdvanceStepperAxis::getHomeDirectionPositive() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 257 | `    return config.homeDirectionPositive;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 258 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 259 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 260 | `float AdvanceStepperAxis::getHomingSeekSpeedMmS() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 261 | `    return config.homingSeekSpeedMmS;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 262 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 263 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 264 | `float AdvanceStepperAxis::getHomingLatchSpeedMmS() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 265 | `    return config.homingLatchSpeedMmS;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 266 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 267 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 268 | `float AdvanceStepperAxis::getHomingBackoffMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 269 | `    return config.homingBackoffMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 270 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 271 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 272 | `float AdvanceStepperAxis::getMaxHomingTravelMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 273 | `    return config.maxHomingTravelMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 274 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 275 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 276 | `float AdvanceStepperAxis::getHomePositionMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 277 | `    return config.homePositionMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 278 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 279 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 280 | `bool AdvanceStepperAxis::isHomeSimulationEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 281 | `    return config.homeSimulationEnabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 282 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 283 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 284 | `void AdvanceStepperAxis::setHomeSimulationEnabled(bool enabled) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 285 | `    config.homeSimulationEnabled = enabled;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 286 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 287 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 288 | `void AdvanceStepperAxis::setSimulatedHomeActive(bool active) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 289 | `    simulatedHomeActive = active;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 290 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 291 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 292 | `bool AdvanceStepperAxis::isHomed() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 293 | `    return homed;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 294 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 295 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 296 | `void AdvanceStepperAxis::setHomed(bool state) {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 297 | `    homed = state;` | Asigna o configura un valor; en recetas define un parametro usado por el firmware. |
| 298 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 299 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 300 | `bool AdvanceStepperAxis::areSoftLimitsEnabled() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 301 | `    return config.softLimitsEnabled;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 302 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 303 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 304 | `bool AdvanceStepperAxis::isTargetWithinSoftLimits(float targetMm) const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 305 | `    if (!config.softLimitsEnabled) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 306 | `        return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 307 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 308 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 309 | `    if (targetMm < config.softLimitMinMm) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 310 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 311 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 312 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 313 | `    if (targetMm > config.softLimitMaxMm) {` | Evalua una condicion para decidir si ejecutar el bloque siguiente. |
| 314 | `        return false;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 315 | `    }` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 316 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 317 | `    return true;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 318 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 319 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 320 | `float AdvanceStepperAxis::getSoftLimitMinMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 321 | `    return config.softLimitMinMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 322 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
| 323 | ` ` | Linea en blanco usada para separar bloques y mejorar lectura. |
| 324 | `float AdvanceStepperAxis::getSoftLimitMaxMm() const {` | Declara o implementa una funcion/metodo; sus parametros controlan la operacion y su retorno informa resultado o dato. |
| 325 | `    return config.softLimitMaxMm;` | Finaliza la funcion devolviendo el valor o estado indicado. |
| 326 | `}` | Instruccion de codigo/configuracion que participa en el flujo descrito por el bloque donde aparece. |
